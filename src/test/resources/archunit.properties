# Restrict ArchUnit's import to this project's own classes — without this it
# transitively scans the entire classpath (Spring, JLine, …), producing
# thousands of DEBUG lines and slowing the layering check for no benefit.
resolveMissingDependenciesFromClassPath=false
